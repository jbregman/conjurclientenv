VOLUMES=-v ~/.aws:/root/.aws

ENV = \
  -e AWS_SUBNET=$(AWS_SUBNET) \
  -e AWS_SECURITY_GROUP=$(AWS_SG) \
  -e CONJUR_HOST=$(CONJUR_HOST) \
  -e CONJUR_USER=$(CONJUR_USER) \
  -e CONJUR_PASSWORD=$(CONJUR_PASSWORD) \
